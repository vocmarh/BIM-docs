# План реализации (WBS)

## Фаза 0. Каркас

- **Схема БД**  
  Используется существующая отработанная схема выгрузки Revit → MSSQL (таблицы `EL_Элементы`, `EL_EAV` и др.), применяемая на всех проектах. Дополнительного проектирования базы данных не требуется.  

- **Роли пользователей**  
  - **БИМ-отдел**  
    - отвечает за выгрузку параметров в БД;  
    - на первом этапе заполняет параметр `TOUCH_Классификатор`.  
  - **Проектировщик** (планируется в будущем)  
    - будет заполнять параметр `TOUCH_Классификатор`.  
  - **Начальник участка / ПТО**  
    - в Revit вручную заполняет `TOUCH_Фактическая_дата_начала`;  
    - в Revit вручную заполняет `TOUCH_Фактическая_дата_завершения`.  
  - **Инструмент (плагин)**  
    - автоматически заполняет `TOUCH_Плановая_дата_начала`;  
    - автоматически заполняет `TOUCH_Плановая_дата_завершения`;  
    - источником данных является ГПР и параметр `TOUCH_Классификатор`.  


## Фаза 1. Данные и модель

### Новые параметры в Revit:

- `TOUCH_Фактическая_дата_начала` — **начальник участка** (ручной ввод).  
- `TOUCH_Фактическая_дата_завершения` — **начальник участка** (ручной ввод).  
- `TOUCH_Плановая_дата_начала` — **плагин** (автоматическое заполнение на основе ГПР и классификатора).  
- `TOUCH_Плановая_дата_завершения` — **плагин** (автоматическое заполнение на основе ГПР и классификатора).  
- `TOUCH_Классификатор` — на первом этапе **БИМ-отдел**, в дальнейшем **проектировщик**.  

### Логика работы:

1. При выгрузке данных из Revit параметры сохраняются в существующую схему БД (EL_Элементы, EL_EAV).  
2. Начальник участка отвечает только за фактические даты.  
3. Плановые даты подтягиваются автоматически, исключая ручной ввод.  
4. Классификатор — ключ для сопоставления элементов с ГПР.  

## Фаза 2. Связка с ГПР (плагин + MSSQL)

**Цель:** подгружать плановые даты и данные задач из ГПР (MS Project → Excel) в базу данных с версионированием и заполнять ими параметры элементов в Revit.

### Источник данных
- Экспорт из MS Project в **Excel** (CSV/XLSX).
- Обязательные поля (в Excel могут называться иначе, важно содержание):
  - `TOUCH_Code` — код по классификатору (ключ связи с элементом Revit);
  - `TaskName` — название задачи;
  - `StartDate` / `FinishDate` — плановые даты начала и окончания;
  - `SDR` — номер работы (внутренний идентификатор задачи).
- Опциональные поля:
  - `Duration` — длительность процесса (в днях или часах);
  - `Responsible` — ответственный за процесс.

### Схема данных в MSSQL (минимум)
**ScheduleVersion** — реестр версий ГПР  
- `VersionId` (PK, авто) — идентификатор версии;  
- `SourceFile` — имя или путь к Excel;  
- `VersionTag` — например, `GPR_2025-10-05_v1`;  
- `ImportedAtUtc` — дата и время импорта;  
- `ImportedBy` — кто импортировал;  
- `IsActive` — признак активной версии.  

**ScheduleBindings** — строки ГПР для конкретной версии  
- `VersionId` (FK → ScheduleVersion);  
- `TOUCH_Code`, `TaskName`, `StartDate`, `FinishDate`, `SDR`;  
- (опц.) `Duration`, `Responsible`.  
Тип дат — `DATE` (без времени), чтобы исключить путаницу с часовыми поясами.

### Политика версий
- По умолчанию активной считается последняя импортированная версия (`MAX(VersionId)`).
- В плагине предусмотрено два режима:
  - **Auto** — использовать последнюю активную версию;
  - **Manual** — пользователь выбирает конкретную версию из списка.

### Процесс (логика)
1. Пользователь выбирает Excel-файл ГПР.  
2. Плагин парсит файл и проверяет наличие обязательных полей.  
3. Создаётся запись в `ScheduleVersion` (фиксируется файл, тег версии, дата импорта).  
4. Все строки загружаются в `ScheduleBindings`.  
5. Формируется словарь:  
   `TOUCH_Code → (StartDate, FinishDate, TaskName, SDR)`  
6. Плагин проходит по элементам модели Revit:
   - Берёт параметр `TOUCH_Классификатор`;
   - Если код найден в ГПР — записывает:
     - `TOUCH_Плановая_дата_начала = StartDate`;
     - `TOUCH_Плановая_дата_завершения = FinishDate`;
     - (опц.) `TOUCH_ГПР_Версия = VersionTag`;
     - (опц.) `TOUCH_СДР = SDR`.
7. История импортов сохраняется в БД; в модели фиксируется версия, по которой были заполнены даты.

### Валидация
- Пустые или некорректные коды — пропускаются и попадают в отчёт.  
- `StartDate > FinishDate` — строка игнорируется.  
- Дубли по одному классификатору — используется правило (например, брать последнюю по порядку).  
- Нет кода в модели — элемент пропускается.

### Роли
- **BIM-отдел** — подготавливает Excel-выгрузку и запускает импорт.  
- **Плагин** — импортирует данные, создаёт версию, сопоставляет по `TOUCH_Code`, записывает даты и метку версии.  
- **Power BI (опц.)** — использует таблицы `ScheduleVersion` и `ScheduleBindings` для отчётов и анализа версий.

### Почему так
- Централизованная история версий ГПР.  
- Простая логика в Revit — данные берутся из стабильного источника.  
- Готово к автоматизации (например, через n8n) без изменений в плагине.


---

## Фаза 2.1. Упрощённый вариант без БД

**Цель:** использовать ГПР из одного Excel-файла без хранения в базе данных — напрямую в плагине Revit.

### Логика процесса
1. Пользователь выбирает Excel-файл ГПР.  
2. Плагин считывает данные напрямую из файла по тем же полям:
   - «Классификатор», «Название», «Начало», «Окончание», «СДР»  
     (опц. «Длительность», «Ответственный»).  
3. Выполняется валидация и очистка данных:
   - Проверка обязательных полей;
   - Проверка дат (`StartDate ≤ FinishDate`);
   - Игнорирование пустых или ошибочных строк.  
4. Формируется словарь в памяти:  
   `TOUCH_Code → (StartDate, FinishDate, TaskName, SDR)`  
5. Плагин сопоставляет данные с моделью Revit и:
   - Подсвечивает элементы с найденными или отсутствующими кодами;
   - (опц.) Записывает `StartDate` и `FinishDate` в параметры Revit;
   - Выводит отчёт: «Найдено N элементов, не найдено M».  
6. (опц.) Экспортирует результат в CSV или Excel.

### Особенности
- Не создаёт записей в БД и не требует версионирования.  
- Подходит для быстрой проверки или разовой связки модели с ГПР.  
- Вся логика работает в памяти, результаты фиксируются только в Revit.

### Почему так
- Минимальный вход — только Excel-файл.  
- Удобно для тестов, демонстраций и пилотов.  
- Позволяет начать работу до развёртывания БД.



### Фаза 3. Обновления моделей  
**Цель:** автоматическое перенесение параметров `TOUCH_*` из старой версии модели в новую при поступлении обновлений от проектировщика.  

#### Логика работы
В процессе рабочей документации проектировщик периодически передает обновленные версии моделей. Несмотря на то, что на более поздних этапах проектировщики (или БИМ-отдел заказчика) будут заполнять **коды по классификатору**, они **не заполняют плановые и фактические даты работ**.  
Чтобы не вводить эти данные вручную при каждом обновлении, используется плагин **«Сравнение модели»**.

#### Алгоритм
1. **Получение новой модели** от проектировщика.  
   БИМ-отдел загружает новую версию модели в Revit.  

2. **Сравнение с предыдущей версией.**  
   Плагин «Сравнение модели» выполняет сопоставление элементов новой модели с элементами предыдущей:
   - в первую очередь — по `ElementGuid`;  
   - при отсутствии `ElementGuid` — по **геометрическим сигнатурам** (форма, координаты, габариты).  

3. **Перенос параметров.**  
   Для найденных совпадений плагин автоматически переносит значения параметров `TOUCH_*` (включая `TOUCH_Плановая_дата_начала`, `TOUCH_Фактическая_дата_окончания`, `TOUCH_Секция`, `TOUCH_Этаж` и т.п.) из старой модели в новую.  

4. **Подготовка модели к работе.**  
   В результате БИМ-отдел получает актуализированную модель, уже содержащую все ранее заполненные параметры, кроме новых элементов.  

5. **Обработка новых элементов.**  
   Начальник участка заполняет **фактические даты** только для новых элементов, появившихся в модели после обновления.  

#### Результат
- Модели сохраняют преемственность данных между версиями.  
- Исключается дублирование работы при повторном заполнении параметров.  
- Обеспечивается актуальность фактических данных в проекте без потери истории изменений.


### Фаза 4. Визуализация (Power BI RS)

**Цель:**  
Создать систему визуализации статусов и прогресса выполнения работ на основе данных из моделей Revit и ГПР, представленных в MS SQL.

---

#### Основная логика

Power BI используется как **витрина данных**, где объединяются:
- табличные данные из Revit (экспортированные через плагин TOUCH);
- данные о плановых и фактических датах из ГПР (через БД);
- визуальная 3D-модель из **Tangl Space** для пространственного анализа.

Для сопоставления табличных данных и 3D-элементов в Tangl Space используется **дополнительный уникальный ключ** (например, `ElementGuid`, `TouchId` или `ClassifierCode`), который задаётся один раз при первичной настройке проекта.  
Это позволяет на уровне Power BI связать элементы таблиц с конкретными объектами в 3D-модели.

---

#### Структура витрин данных

| Витрина | Назначение |
|----------|-------------|
| **vw_Elements** | Основные данные по элементам (код классификатора, секция, этаж, категория, объем и т.д.). |
| **vw_PlanFact** | Плановые и фактические даты выполнения, проценты выполнения, отклонения. |
| **vw_StatusAgg** | Агрегированные данные для KPI и сводных отчетов (по секциям, категориям, периодам). |

---

#### Визуальные представления в Power BI

1. **3D-раскраска статусов**  
   - Интеграция с Tangl Space позволяет визуально отображать элементы по статусу:  
     - зелёный — выполнено,  
     - жёлтый — в процессе,  
     - серый — не начато,  
     - красный — отклонение от плана.  
   - Состояние обновляется автоматически на основании данных из `vw_PlanFact`.

2. **Таймлайн строительства**  
   - Линейный график по датам с возможностью фильтрации по секции, этажу или категории.  
   - Показывает план-факт выполнения и динамику отклонений во времени.

3. **KPI-дашборд**  
   - Сводные показатели:  
     - % выполнения по объекту/секции,  
     - объём выполненных работ в м³/м²,  
     - отклонения от графика (дней, %).  
   - Используется для контроля производственных темпов и выявления рисков.

---

#### Пример визуализации

📊 [Пример связки 3D-модели с таблицей (выполненные объёмы по дням)](https://app.powerbi.com/view?r=eyJrIjoiZGQ2ZjVkMzctMGMyZi00YTJmLTllM2MtODliM2U4OWIyYTk1IiwidCI6ImVhZGVjNTc5LWQyOTktNDE2Ny04OTY2LWFhMjY1YTdjNjRkYyIsImMiOjN9)

---

#### Результат

- Единая визуальная платформа для анализа план-фактных данных.  
- Возможность пространственного анализа (3D-контекст).  
- Прозрачная оценка статуса строительства и динамики отклонений.  
- Автоматическая актуализация данных при обновлении моделей Revit или ГПР.


### Фаза 5. Презентационные материалы
**Цель:**  
Сделать использование плагина максимально простым и понятным для конечных пользователей (начальников участков, инженеров ПТО, BIM-координаторов), а также зафиксировать результаты в виде обучающих материалов.

---

#### Основная логика

1. **Обучение и внедрение:**  
   - Подготовить **презентацию** — краткое руководство с визуальными схемами и основными шагами работы в плагине.  
   - Записать **5-минутное обучающее видео**, демонстрирующее:
     - загрузку данных из модели;
     - работу с параметрами TOUCH;
     - заполнение план-фактных дат;
     - передачу данных в Power BI.  
   - Видео и памятка должны быть понятны без дополнительного объяснения.

2. **Презентация результатов:**  
   - Провести короткую **презентацию проекта** (5–10 минут) для демонстрации итогов работы плагина, логики интеграции и визуализации в Power BI.  
   - Основной акцент — **простота и прозрачность** взаимодействия между Revit, БД и Power BI.

---

#### Результат

- Полный комплект обучающих материалов (видео + памятка).  
- Возможность масштабировать решение на другие объекты без дополнительного обучения.


> Для каждой задачи ведите DoD и тесты в `/docs/04.acceptance.md`.
